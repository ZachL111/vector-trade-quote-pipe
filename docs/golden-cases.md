# Golden Cases

The golden set keeps `spread pressure` and `portfolio drift` visible as named cases.

The main golden fixture is `fixtures/golden/scoreboard.csv`. The matrix fixture is `fixtures/golden/lane-matrix.csv`. Together they cover `spread pressure`, `fill risk`, `portfolio drift`, and `quote width` with different score ranges.

The purpose is to make intentional rule changes show up in review.
